# VAD Phase 1.5 — homonīmu cleanup + retry + Hosams override

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atrisināt 3 deploy-blocking problēmas, kas atklātas pēc 2026-05-02 production smoke + 152-politiķu sweep (skat. spec § 15.2 F13/F14 + handoff `docs/superpowers/handoff-vad-phase-1.5.md`): (1) homonīmu kontaminācija 11 pidiem ar identiska V+U vārdiem (1221 dekl ar dažādu cilvēku datiem zem mūsu opponent_id); (2) parse-fail uz 1304 UUIDs (anti-scrape nonce invalidation); (3) Hosams Abu Meri name split override.

**Architecture:** A2 (DB-driven) disambiguation — `tracked_politicians.keywords` JSON dabū jaunu `vad_disambig` lauku ar pozitīvo substring whitelist per pid. Filter rule: ja pid ir `vad_disambig` saraksts, search-row tiek pieņemts ja vismaz viens substring (case-ins) match `r.institution` vai `r.position_title`. Reuse esošo `negative_patterns` kā override-reject (pid=146 jau ir `bijušais prezidents` u.c.). Bez hints → trust full-name search (pašreizējā uzvedība, droša unikāliem vārdiem). Retry: orchestrator pievieno `client.reset_session()` callback un re-search + re-fetch ar svaigu UUID, ja parse pirmo reizi fail. Hosams: viens entry `_NAME_OVERRIDES` dictā.

**Tech Stack:** Python 3.11+, SQLite, pytest, httpx (existing). Nav jaunu deps. Schema migration NAV vajadzīga — `keywords` JSON kolonnā jau eksistē, papildinām esošā JSON sarakstā jaunu lauku.

---

## File Structure

**Modify:**
- `src/vad/matcher.py` — `_NAME_OVERRIDES` jauns entry pid 161
- `src/vad/declarations.py` — jauns `_load_disambig_hints(db, pid)` helper + filter rule `fetch_for_politician`
- `src/vad/fetch.py` — `VadClient.reset_session()` jauna metode
- `scripts/ingest_vad_declarations.py` — `Path("logs").mkdir(exist_ok=True)` skripta sākumā

**Create:**
- `scripts/seed_vad_disambig.py` — vienreizēja curator skripts, kas ieliek 11 pidiem `vad_disambig` JSON
- `scripts/cleanup_contaminated_vad.py` — DELETE + re-ingest 11 pidiem ar dry-run flag

**Test:**
- `tests/test_vad_matcher.py` — pievienot test_split_hosams_override
- `tests/test_vad_declarations.py` — jauni testi disambig filter + retry logic
- `tests/test_vad_fetch.py` — pievienot reset_session test

**Plan + audit:**
- `docs/superpowers/plans/2026-05-02-vad-phase-1.5.md` (šis fails)
- `docs/superpowers/specs/2026-05-02-vad-deklaracijas-design.md` § 15.3 jauns audit trail Phase 1.5 atklājumiem (pievienojams pēc fix'iem, ne plana fāzē)
- `wiki/CHANGELOG.md` — jauns ieraksts pēc merge

---

## Task 1: Hosams Abu Meri name override

**Files:**
- Modify: `src/vad/matcher.py:17` (_NAME_OVERRIDES dict)
- Test: `tests/test_vad_matcher.py`

- [ ] **Step 1: Write failing test**

Pievienot `tests/test_vad_matcher.py` beigās:

```python
def test_split_hosams_override():
    """pid 161 Hosams Abu Meri — naïve dotu ('Hosams Abu', 'Meri') kas ir nepareizi.
    Override pid-keyed _NAME_OVERRIDES dictā labos uz ('Hosams', 'Abu Meri').
    """
    assert split_name(161, "Hosams Abu Meri") == ("Hosams", "Abu Meri")
```

- [ ] **Step 2: Run test, verify FAIL**

```
cd ~\atmina\.worktrees\vad-phase-1.5
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_matcher.py::test_split_hosams_override -v
```

Expected: FAIL — `assert ('Hosams Abu', 'Meri') == ('Hosams', 'Abu Meri')`

- [ ] **Step 3: Implement override**

Edit `src/vad/matcher.py:17`:

```python
_NAME_OVERRIDES: dict[int, tuple[str, str]] = {
    161: ("Hosams", "Abu Meri"),  # Veselības ministrs (Phase 1.5 — VID search bez override atgriež 0)
}
```

- [ ] **Step 4: Run test, verify PASS**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_matcher.py -v
```

Expected: 8 passed (visi vad_matcher testi).

- [ ] **Step 5: Commit**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_matcher.py -v
git add src/vad/matcher.py tests/test_vad_matcher.py
@'
fix(vad): _NAME_OVERRIDES[161] for Hosams Abu Meri

Phase 1.5 — naïve split daļās 3-token vārdu "Hosams Abu Meri" uz
("Hosams Abu", "Meri") kas dod 0 VID search rezultātus. Pareizi ir
("Hosams", "Abu Meri") kā Veselības ministra deklarācijās VID portālā.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 2: VAD disambig filter — schema test prep + helper

**Files:**
- Modify: `tests/test_vad_declarations.py` `_make_db()` — pievienot `keywords` un `negative_patterns` kolonnas testa schema

- [ ] **Step 1: Modify test fixture schema**

Edit `tests/test_vad_declarations.py:27-34` (_make_db CREATE TABLE):

```python
    db.executescript("""
        CREATE TABLE tracked_politicians (
            id INTEGER PRIMARY KEY,
            name TEXT NOT NULL,
            role TEXT,
            keywords TEXT DEFAULT '[]',
            negative_patterns TEXT
        );
        INSERT INTO tracked_politicians(id, name, role) VALUES (3, 'Ainārs Šlesers', 'Saeimas deputāts');
    """)
```

- [ ] **Step 2: Run existing tests, verify still PASS**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_declarations.py -v
```

Expected: 5 passed (visi esošie testi joprojām strādā).

- [ ] **Step 3: Commit**

```
git add tests/test_vad_declarations.py
@'
test(vad): add keywords + negative_patterns to test fixture

Phase 1.5 sagatavošana — disambig filter lasīs no keywords JSON; testa
fixture vajag kolonnas, lai jaunie testi var ielādēt vad_disambig.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 3: VAD disambig filter — failing tests

**Files:**
- Test: `tests/test_vad_declarations.py`

- [ ] **Step 1: Add 3 failing tests**

Append `tests/test_vad_declarations.py`:

```python
import json


def _make_db_with_keywords(keywords: list[str], negative_patterns: list[str] | None = None):
    """Like _make_db, but populates pid 3's vad_disambig + neg_patterns."""
    db, path = _make_db()
    db.execute(
        "UPDATE tracked_politicians SET keywords=?, negative_patterns=? WHERE id=3",
        (json.dumps({"vad_disambig": keywords}), json.dumps(negative_patterns) if negative_patterns else None),
    )
    db.commit()
    return db, path


def test_disambig_accepts_row_with_substring_match():
    """vad_disambig=['Saeimas deputāts'] → row ar position='Saeimas deputāts' tiek pieņemts."""
    db, path = _make_db_with_keywords(["Saeimas deputāts"])
    try:
        client = _mock_client_with_one_row()  # row ir Saeimas deputāts
        result = fetch_for_politician(3, db, client)
        assert result.new_inserted == 1
        assert result.rows_skipped_role == 0
    finally:
        db.close()
        _safe_unlink(path)


def test_disambig_rejects_row_without_match():
    """vad_disambig=['Ministru kabinets'] (substring no row institution/position) → reject."""
    db, path = _make_db_with_keywords(["Ministru kabinets"])
    try:
        client = _mock_client_with_one_row()  # row ir Saeima/Saeimas deputāts
        result = fetch_for_politician(3, db, client)
        assert result.new_inserted == 0
        assert result.rows_skipped_role == 1
    finally:
        db.close()
        _safe_unlink(path)


def test_disambig_negative_pattern_overrides_positive():
    """vad_disambig match BUT negative_patterns also match → reject (negative wins)."""
    db, path = _make_db_with_keywords(
        keywords=["Saeimas deputāts"],
        negative_patterns=["Latvijas Republikas Saeima"],
    )
    try:
        client = _mock_client_with_one_row()
        result = fetch_for_politician(3, db, client)
        assert result.new_inserted == 0
        assert result.rows_skipped_role == 1
    finally:
        db.close()
        _safe_unlink(path)


def test_disambig_empty_hints_passes_through():
    """vad_disambig=[] vai NULL → trust full-name search, accept all rows (current behaviour)."""
    db, path = _make_db_with_keywords([])  # explicit empty
    try:
        client = _mock_client_with_one_row()
        result = fetch_for_politician(3, db, client)
        assert result.new_inserted == 1
        assert result.rows_skipped_role == 0
    finally:
        db.close()
        _safe_unlink(path)
```

- [ ] **Step 2: Run, verify 3-4 fail**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_declarations.py -v -k disambig
```

Expected: 4 tests; passes for `accepts_row_with_substring_match` un `empty_hints_passes_through` (default = accept), fails for `rejects_row_without_match` un `negative_pattern_overrides_positive` (no filter aktīvs).

---

## Task 4: VAD disambig filter — implementation

**Files:**
- Modify: `src/vad/declarations.py`

- [ ] **Step 1: Add disambig helpers**

Edit `src/vad/declarations.py` — pievieno pirms `fetch_for_politician`:

```python
import json


def _load_disambig_config(
    db: sqlite3.Connection, opponent_id: int
) -> tuple[list[str], list[str]]:
    """Load (vad_disambig hints, negative_patterns) for a politician.

    vad_disambig dzīvo `tracked_politicians.keywords` JSON kā `vad_disambig` lauks.
    negative_patterns dzīvo savā kolonnā kā JSON list. Abi tukši → ([], []).
    Tukšs disambig saraksts → trust full-name search (skip filtra).
    """
    row = db.execute(
        "SELECT keywords, negative_patterns FROM tracked_politicians WHERE id=?",
        (opponent_id,),
    ).fetchone()
    if row is None:
        return [], []
    hints: list[str] = []
    if row["keywords"]:
        try:
            kw = json.loads(row["keywords"])
            if isinstance(kw, dict):
                hints = list(kw.get("vad_disambig") or [])
            # NB: ja keywords ir saraksts (legacy formāts), nav vad_disambig — paliek []
        except (json.JSONDecodeError, TypeError):
            pass
    neg: list[str] = []
    if row["negative_patterns"]:
        try:
            np = json.loads(row["negative_patterns"])
            if isinstance(np, list):
                neg = list(np)
        except (json.JSONDecodeError, TypeError):
            pass
    return hints, neg


def _row_passes_disambig(
    row: SearchResultRow, hints: list[str], neg_patterns: list[str]
) -> bool:
    """True iff row tiek pieņemts pēc disambig filter rules.

    Rule 1: ja hints tukši → True (no filter).
    Rule 2: ja kāds neg_pattern (case-ins substring) atrasts inst+pos → False.
    Rule 3: vismaz viens hint (case-ins substring) jābūt inst vai pos → True; citādi False.
    """
    if not hints:
        return True
    haystack = f"{row.institution} {row.position_title}".lower()
    for neg in neg_patterns:
        if neg and neg.lower() in haystack:
            return False
    for hint in hints:
        if hint and hint.lower() in haystack:
            return True
    return False
```

- [ ] **Step 2: Wire up in fetch_for_politician**

Edit `src/vad/declarations.py` `fetch_for_politician` (loc ~83): pievieno disambig load pirms `existing_keys`, izmanto `_row_passes_disambig` row loop:

```python
    result = StoreResult(opponent_id=opponent_id, politician_name=name, rows_found=0)

    rows: list[SearchResultRow] = []
    for given, family in candidate_name_pairs(opponent_id, name):
        rows = client.search(given, family)
        if rows:
            break

    # Phase 1.5 — disambig filter (homonīmu kontaminācija) ielādē per-pid hints
    disambig_hints, disambig_neg = _load_disambig_config(db, opponent_id)

    # Pre-load existing natural keys for cheap dedup (no detail fetch).
    existing_keys = {
        ...  # nemainās
    }

    for r in rows:
        if r.is_legacy and not include_legacy:
            result.rows_skipped_legacy += 1
            continue
        if not _row_passes_disambig(r, disambig_hints, disambig_neg):
            log.warning(
                "vad-disambig-skip: pid=%d name=%r vid_inst=%r vid_pos=%r hints=%r neg=%r",
                opponent_id, name, r.institution, r.position_title,
                disambig_hints, disambig_neg,
            )
            result.rows_skipped_role += 1
            continue
        if not role_matches(role, r.institution, r.position_title):
            ...  # esošā loģika
```

`role_matches` paliek (vienmēr True) backward-compat dēļ — testi joprojām verificē.

- [ ] **Step 3: Run all vad_declarations tests**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_declarations.py -v
```

Expected: 9 passed (5 esošie + 4 jauni disambig).

- [ ] **Step 4: Commit**

```
git add src/vad/declarations.py tests/test_vad_declarations.py
@'
feat(vad): A2 disambig filter via tracked_politicians.keywords.vad_disambig

Phase 1.5 F13 — homonīmu kontaminācija atrisināta DB-driven mehānisma:
keywords JSON dabū vad_disambig substring saraksts; row tiek pieņemts ja
kāds substring match institution VAI position_title (case-ins). Esošais
negative_patterns lauks override-reject (negatīvais uzvar). Tukši hints →
trust full-name search (pašreizējā uzvedība, droša unikāliem vārdiem).

Skat. spec § 15.2 F13 + plan T3+T4.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 5: VadClient.reset_session() + parse-fail retry

**Files:**
- Modify: `src/vad/fetch.py` (jauna `reset_session` metode)
- Modify: `src/vad/declarations.py` (orchestrator retry hook)
- Test: `tests/test_vad_fetch.py` (jauns reset test) + `tests/test_vad_declarations.py` (retry test)

- [ ] **Step 1: Write failing test for reset_session**

Append `tests/test_vad_fetch.py`:

```python
def test_reset_session_clears_initialized_flag():
    """After reset_session, next call _ensure_session() must re-visit /VAD."""
    client = VadClient(throttle=False)
    # Simulate prior session bootstrap
    client._session_initialized = True
    client.reset_session()
    assert client._session_initialized is False
```

- [ ] **Step 2: Run, verify FAIL**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_fetch.py::test_reset_session_clears_initialized_flag -v
```

Expected: FAIL — `AttributeError: 'VadClient' has no attribute 'reset_session'`.

- [ ] **Step 3: Implement reset_session**

Edit `src/vad/fetch.py` `VadClient` (after `close`):

```python
    def reset_session(self):
        """Force next call to re-bootstrap session (visit /VAD).

        Phase 1.5 — anti-scrape mehanisms invalidates UUID nonces pēc N rapid
        sequential requests; retry pēc reset + re-search dod svaigus UUIDs.
        """
        self._session_initialized = False
        # Clear any session cookies that may be tied to invalidated state
        self._client.cookies.clear()
```

- [ ] **Step 4: Run, verify PASS**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_fetch.py -v
```

Expected: existing + new test all pass.

- [ ] **Step 5: Write failing test for orchestrator retry**

Append `tests/test_vad_declarations.py`:

```python
def test_fetch_for_politician_retries_on_parse_fail():
    """Parse fail (no header table) → reset session, re-search, fetch with new UUID."""
    db, path = _make_db()
    try:
        client = MagicMock()
        # First search returns row with stale UUID
        # Second search (after reset) returns row with fresh UUID
        first_row = SearchResultRow(
            vad_uuid="stale-uuid", declaration_type="Kārtējā gada deklarācija - par 2024. gadu",
            is_legacy=False, institution="Latvijas Republikas Saeima",
            position_title="Saeimas deputāts",
        )
        fresh_row = SearchResultRow(
            vad_uuid="fresh-uuid", declaration_type="Kārtējā gada deklarācija - par 2024. gadu",
            is_legacy=False, institution="Latvijas Republikas Saeima",
            position_title="Saeimas deputāts",
        )
        client.search.side_effect = [[first_row], [fresh_row]]
        # First fetch returns broken HTML (no <table>); second returns valid
        broken_html = "<html><body>Anti-scrape redirect</body></html>"
        client.fetch_detail.side_effect = [broken_html, FIXTURE_HTML]
        result = fetch_for_politician(3, db, client)
        assert result.new_inserted == 1
        assert client.reset_session.called
        # Second search must have happened (retry)
        assert client.search.call_count == 2
    finally:
        db.close()
        _safe_unlink(path)
```

- [ ] **Step 6: Run, verify FAIL**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_declarations.py::test_fetch_for_politician_retries_on_parse_fail -v
```

Expected: FAIL — first fetch raises ValueError, no retry yet.

- [ ] **Step 7: Implement retry in orchestrator**

Edit `src/vad/declarations.py` — `fetch_for_politician` row loop, replace existing try/except:

```python
        try:
            html = client.fetch_detail(r.vad_uuid)
            parsed = parse_declaration_html(html)
        except ValueError as e:
            if "nav header table" in str(e):
                log.warning(
                    "vad-parse-fail retry: pid=%d uuid=%s — reset+re-search",
                    opponent_id, r.vad_uuid,
                )
                client.reset_session()
                rows_retry = client.search(*next(iter(candidate_name_pairs(opponent_id, name))))
                fresh = next(
                    (rr for rr in rows_retry
                     if _norm_kind_from_label(rr.declaration_type) == kind
                     and _year_from_label(rr.declaration_type) == year
                     and rr.position_title == r.position_title),
                    None,
                )
                if fresh is None:
                    msg = f"uuid={r.vad_uuid}: parse fail; retry search dod nav match"
                    log.warning("vad-fetch-fail %s", msg)
                    result.errors.append(msg)
                    continue
                try:
                    html = client.fetch_detail(fresh.vad_uuid)
                    parsed = parse_declaration_html(html)
                    r = fresh  # use fresh row for storage (vad_uuid)
                except Exception as e2:
                    msg = f"uuid={r.vad_uuid}->retry={fresh.vad_uuid}: {type(e2).__name__}: {e2}"
                    log.warning("vad-fetch-fail-after-retry %s", msg)
                    result.errors.append(msg)
                    continue
            else:
                msg = f"uuid={r.vad_uuid}: ValueError: {e}"
                log.warning("vad-fetch-fail %s", msg)
                result.errors.append(msg)
                continue
        except Exception as e:
            msg = f"uuid={r.vad_uuid}: {type(e).__name__}: {e}"
            log.exception("vad-fetch-fail %s", msg)
            result.errors.append(msg)
            continue
        try:
            _store(db, opponent_id, r, parsed, html, name)
            existing_keys.add(natural_key)
            result.new_inserted += 1
        except Exception as e:
            msg = f"uuid={r.vad_uuid}: store {type(e).__name__}: {e}"
            log.exception("vad-store-fail %s", msg)
            result.errors.append(msg)
```

NB: `dry_run` ceļš nesedz retry — tikai dry_run nofetcho (paliek esošā loģika).

- [ ] **Step 8: Run, verify PASS**

```
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_declarations.py -v
& '~\atmina\.venv\Scripts\python.exe' -m pytest tests/test_vad_fetch.py -v
```

Expected: visi pass (10 declarations + N fetch testi).

- [ ] **Step 9: Commit**

```
git add src/vad/fetch.py src/vad/declarations.py tests/test_vad_fetch.py tests/test_vad_declarations.py
@'
feat(vad): retry parse-fail with reset_session + re-search

Phase 1.5 F14 — VID anti-scrape invalidates UUID nonces pēc N rapid
sequential requests; ~1304 sweep parse-fails dotā vakara session. Tagad
orchestrator catches ValueError "nav header table", calls
client.reset_session() (clear cookies + force /VAD re-visit), re-issues
search (svaigi UUIDs), atrod matching natural-key row, retry detail fetch.
Max viens retry per row.

Skat. spec § 15.2 F14 + plan T5.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 6: ingest_vad_declarations.py — logs/ mkdir

**Files:**
- Modify: `scripts/ingest_vad_declarations.py`

- [ ] **Step 1: Add mkdir at script entry**

Edit `scripts/ingest_vad_declarations.py:54` (sākumā `main()`):

```python
def main(argv=None) -> int:
    p = argparse.ArgumentParser(description=__doc__.split("\n\n")[0])
    p.add_argument("--politician", help="slug or name substring; default = visi tracked")
    p.add_argument("--limit", type=int, help="max politiķi per palaišana")
    p.add_argument("--dry-run", action="store_true", help="parser+match, bet nav DB write")
    p.add_argument("--include-legacy", action="store_true",
                   help="iekļauj /VAD2002Data legacy deklarācijas (Phase 0.5 backlog)")
    args = p.parse_args(argv)

    # Phase 1.5 F15 — ensure logs/ dir exists pirms iespējamā tee redirect
    (REPO_ROOT / "logs").mkdir(exist_ok=True)

    db = get_db()
    init_vad_tables()  # idempotent
```

- [ ] **Step 2: Verify (no test, manual sanity check)**

```
& '~\atmina\.venv\Scripts\python.exe' scripts/ingest_vad_declarations.py --dry-run --limit 1
```

Expected: skripts palaižas, neraisīta crash, log redirektē var izmantot.

- [ ] **Step 3: Commit**

```
git add scripts/ingest_vad_declarations.py
@'
fix(vad): ensure logs/ dir before ingest sweep starts (F15)

Phase 1.5 F15 — sweep mēģināja flush logs uz nepastāvošu logs/ katalogu;
silent crash background task pēc ~15 min. Tagad mkdir(exist_ok=True)
sākumā main(); skripts strādā arī fresh checkout-ā bez priekšējās setup.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 7: Curator skripts — seed_vad_disambig.py + cleanup_contaminated_vad.py

**Files:**
- Create: `scripts/seed_vad_disambig.py`
- Create: `scripts/cleanup_contaminated_vad.py`

- [ ] **Step 1: Create seed_vad_disambig.py**

```python
"""Seed tracked_politicians.keywords.vad_disambig for 11 contaminated pids.

Phase 1.5 F13 — homonīmu V+U gadījumi VID portālā. Skat. plan T7
+ Telegram melnraksts apstiprinājums 2026-05-02 msg 1584.

Rule: keywords JSON kļūst dict ar lauku vad_disambig=[..substrings..].
Ja keywords ir saraksts (legacy formāts), pārveido uz dict.
"""

from __future__ import annotations

import json
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPO_ROOT))

from src.db import get_db  # noqa: E402

# (pid, name, vad_disambig substrings) — apstiprināts 2026-05-02 msg 1584
HINTS: list[tuple[int, str, list[str]]] = [
    (146, "Andris Bērziņš",   ["Saeimas deputāts", "Saeimas deputāte", "Latvijas Republikas Saeima"]),
    (101, "Inese Kalniņa",    ["Saeimas deputāts", "Latvijas Republikas Saeima", "Tiesu administrācija", "Latvijas Nacionālais arhīvs"]),
    (144, "Inga Bērziņa",     ["Saeimas deputāts", "Saeimas deputāte", "Latvijas Republikas Saeima"]),
    (104, "Līga Kļaviņa",     ["Saeimas deputāts", "Saeimas deputāte", "Latvijas Republikas Saeima", "Finanšu ministrija"]),
    (138, "Jānis Zariņš",     ["Saeimas deputāts", "Latvijas Republikas Saeima", "Valsts meža dienests"]),
    (106, "Līga Kozlovska",   ["Saeimas deputāts", "Saeimas deputāte", "Latvijas Republikas Saeima"]),
    (155, "Dace Melbārde",    ["Izglītības un zinātnes ministre", "Kultūras ministre", "Valsts kanceleja", "Saeimas deputāte", "Latvijas Republikas Saeima"]),
    ( 92, "Iļja Ivanovs",     ["Saeimas deputāts", "Latvijas Republikas Saeima"]),
    ( 25, "Viktors Valainis", ["Saeimas deputāts", "Latvijas Republikas Saeima", "Ekonomikas ministrs", "Valsts kanceleja"]),
    (132, "Jānis Skrastiņš",  ["Saeimas deputāts", "Latvijas Republikas Saeima", "Zvērināts notārs", "Tieslietu ministrija"]),
    (107, "Linda Liepiņa",    ["Saeimas deputāts", "Saeimas deputāte", "Latvijas Republikas Saeima", "Korupcijas novēršanas un apkarošanas birojs"]),
]


def main() -> int:
    db = get_db()
    for pid, expected_name, hints in HINTS:
        row = db.execute("SELECT name, keywords FROM tracked_politicians WHERE id=?", (pid,)).fetchone()
        if row is None:
            print(f"[skip] pid={pid} not found in tracked_politicians")
            continue
        if row["name"] != expected_name:
            print(f"[warn] pid={pid} name mismatch: expected {expected_name!r}, got {row['name']!r}")
        # Existing keywords parse
        existing: dict | list = []
        if row["keywords"]:
            try:
                existing = json.loads(row["keywords"])
            except json.JSONDecodeError:
                existing = []
        # Convert legacy list → dict; preserve old list under "tags" key
        if isinstance(existing, list):
            existing = {"tags": existing} if existing else {}
        existing["vad_disambig"] = hints
        new_kw = json.dumps(existing, ensure_ascii=False)
        db.execute("UPDATE tracked_politicians SET keywords=? WHERE id=?", (new_kw, pid))
        print(f"[ok]  pid={pid:>3} {row['name']:<28} vad_disambig={hints}")
    db.commit()
    print(f"\n[done] {len(HINTS)} pids updated")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Create cleanup_contaminated_vad.py**

```python
"""DELETE contaminated VAD declarations + targeted re-ingest.

Phase 1.5 F13 cleanup — 11 pidiem ielādēja sajauktu personu datus
homonīmu V+U dēļ. CASCADE no vad_declarations dzēš visas section rows.
Tālāk: ingest_vad_declarations.py --politician <each> ar svaigu disambig
filter ielādē tikai mūsu reālos politiķus.

Usage:
    python scripts/cleanup_contaminated_vad.py --dry-run   # tikai count
    python scripts/cleanup_contaminated_vad.py             # DELETE
    python scripts/cleanup_contaminated_vad.py --reingest  # DELETE + sweep
"""

from __future__ import annotations

import argparse
import subprocess
import sys
import time
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPO_ROOT))

from src.db import get_db  # noqa: E402

CONTAMINATED_PIDS = [146, 101, 144, 104, 138, 106, 155, 92, 25, 132, 107]


def main() -> int:
    p = argparse.ArgumentParser()
    p.add_argument("--dry-run", action="store_true")
    p.add_argument("--reingest", action="store_true",
                   help="pēc DELETE palaiž ingest_vad_declarations.py per pid")
    args = p.parse_args()

    db = get_db()
    pids_csv = ",".join(str(p) for p in CONTAMINATED_PIDS)
    n_before = db.execute(
        f"SELECT COUNT(*) FROM vad_declarations WHERE opponent_id IN ({pids_csv})"
    ).fetchone()[0]
    print(f"[plan] DELETE {n_before} declarations from {len(CONTAMINATED_PIDS)} pids")

    if args.dry_run:
        print("[dry-run] no changes")
        return 0

    db.execute(f"DELETE FROM vad_declarations WHERE opponent_id IN ({pids_csv})")
    db.commit()
    n_after = db.execute(
        f"SELECT COUNT(*) FROM vad_declarations WHERE opponent_id IN ({pids_csv})"
    ).fetchone()[0]
    print(f"[done] DELETE: {n_before} → {n_after}")

    if not args.reingest:
        return 0

    print(f"\n[reingest] sweep {len(CONTAMINATED_PIDS)} pids ar disambig filter aktīvu")
    for i, pid in enumerate(CONTAMINATED_PIDS, 1):
        row = db.execute("SELECT name FROM tracked_politicians WHERE id=?", (pid,)).fetchone()
        if not row:
            print(f"[skip] pid={pid} not found"); continue
        slug_arg = row["name"]  # ingest matches by name substring too
        t0 = time.monotonic()
        print(f"\n[{i}/{len(CONTAMINATED_PIDS)}] pid={pid} {row['name']}")
        rc = subprocess.call([
            sys.executable, str(REPO_ROOT / "scripts" / "ingest_vad_declarations.py"),
            "--politician", slug_arg,
        ])
        print(f"  exit={rc} ({time.monotonic()-t0:.1f}s)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: Dry-run check**

```
& '~\atmina\.venv\Scripts\python.exe' scripts/seed_vad_disambig.py
```

Expected: 11 [ok] rindas.

```
& '~\atmina\.venv\Scripts\python.exe' scripts/cleanup_contaminated_vad.py --dry-run
```

Expected: `[plan] DELETE 1221 declarations from 11 pids` + `[dry-run] no changes`.

- [ ] **Step 4: Verify hints actually loaded**

```
& '~\atmina\.venv\Scripts\python.exe' -c "import sqlite3, json; con = sqlite3.connect('data/atmina.db'); print(json.loads(con.execute('SELECT keywords FROM tracked_politicians WHERE id=146').fetchone()[0]))"
```

Expected: dict ar `vad_disambig` lauku.

- [ ] **Step 5: Commit**

```
git add scripts/seed_vad_disambig.py scripts/cleanup_contaminated_vad.py
@'
chore(vad): Phase 1.5 curator + cleanup skripti

seed_vad_disambig.py — ielādē 11 kontaminētiem pidiem vad_disambig
substring whitelist (apstiprināts msg 1584). Backward-compat: legacy
list keywords pārveidots uz dict {"tags": [..], "vad_disambig": [..]}.

cleanup_contaminated_vad.py — DELETE no vad_declarations 11 pidiem,
optionāli --reingest karoga sweep ar svaigu disambig filter.

Skat. spec § 15.2 F13 + plan T7.
'@ | Out-File -Encoding utf8 .git-commit-msg.tmp
git commit -F .git-commit-msg.tmp
```

---

## Task 8: Run check.sh — verification gate

**Files:** none (verification only)

- [ ] **Step 1: Run check.sh**

```
Set-Location "~\atmina\.worktrees\vad-phase-1.5"
& "C:\Program Files\Git\bin\bash.exe" scripts/check.sh
```

Expected: `ruff` clean + 914+ tests pass + smoke render OK.

- [ ] **Step 2: Ja kļūdas — fix forward**

Ja ruff/pytest fail: fix kodu, atkārto Step 1. Pēc 2 secīgiem fix attempts → revert un report (CLAUDE.md operating principle 6).

---

## Task 9: Seed disambig hints + DELETE + re-ingest

**Files:** none (DB ops)

- [ ] **Step 1: Seed disambig**

```
& '~\atmina\.venv\Scripts\python.exe' scripts/seed_vad_disambig.py
```

Expected: 11 [ok] rindas.

- [ ] **Step 2: DELETE contaminated**

```
& '~\atmina\.venv\Scripts\python.exe' scripts/cleanup_contaminated_vad.py
```

Expected: `[done] DELETE: 1221 → 0`.

- [ ] **Step 3: Re-ingest 11 pids**

NB: ņem vērā ~10s search throttle + ~3s detail × ~5-15 dekl per pid → ~3-10 min per pid → ~30-60 min kopā. Ja iziet workda time → palaiž `run_in_background=True`.

```
Set-Location "~\atmina\.worktrees\vad-phase-1.5"
& '~\atmina\.venv\Scripts\python.exe' scripts/cleanup_contaminated_vad.py --reingest 2>&1 | Tee-Object -FilePath logs/vad-phase-1.5-reingest.log
```

Expected: visi 11 pids `[ok]` ar `errs=0`. Ja kāds errs >0 — pārbaudīt logs un atkārto cleanup tam pid.

NB: cleanup_contaminated_vad.py jau dzēsa rows no STEP 2; sweep insertē tikai jaunos. Ja sweep palaists pēc DELETE, gaidāms `new=N present=0` per pid.

- [ ] **Step 4: Verify counts**

```
& '~\atmina\.venv\Scripts\python.exe' -c "
import sqlite3
con = sqlite3.connect('data/atmina.db')
con.row_factory = sqlite3.Row
print('=== Post-cleanup pid counts ===')
for pid in [146, 101, 144, 104, 138, 106, 155, 92, 25, 132, 107]:
    n = con.execute('SELECT COUNT(*) FROM vad_declarations WHERE opponent_id=?', (pid,)).fetchone()[0]
    name = con.execute('SELECT name FROM tracked_politicians WHERE id=?', (pid,)).fetchone()[0]
    print(f'  pid={pid:>3}  {name:<28}  n={n}')
print(con.execute('SELECT COUNT(*) FROM vad_declarations').fetchone()[0], 'total')
"
```

Expected: katrs pid <= 30 (reālas dekl per personas reālā Saeima karjēra).

---

## Task 10: Re-render + dry-run deploy preview

**Files:** generate_public_site() output uz `output/`

- [ ] **Step 1: Generate public site**

```
& '~\atmina\.venv\Scripts\python.exe' -c "from src.render import generate_public_site; generate_public_site()"
```

Expected: `output/atmina/` re-built.

- [ ] **Step 2: Smoke check — open one contaminated profile**

```
& '~\atmina\.venv\Scripts\python.exe' -c "
from pathlib import Path
p = Path('output/atmina/politiki/andris-berzins.html')
content = p.read_text(encoding='utf-8')
# Count VAD declarations rendered
import re
ms = re.findall(r'class=\"vad-declaration\"', content)
print(f'andris-berzins.html: {len(ms)} VAD declarations')
"
```

Expected: <30 (vs 228 pre-cleanup).

- [ ] **Step 3: Dry-run deploy preview**

```
Set-Location "~\atmina\.worktrees\vad-phase-1.5"
& "C:\Program Files\Git\bin\bash.exe" scripts/deploy.sh --dry-run
```

Expected: tabula ar to-be-changed files; nav errors.

- [ ] **Step 4: Atskaitīšanās lietotājam Telegrammā**

Sūtīt counts before/after, sample contaminated pid post-cleanup count, deploy --dry-run summary. Gaidīt apstiprinājumu pirms reāla deploy.

---

## Task 11: User approval → real deploy + merge to master

**Files:** none

- [ ] **Step 1: Saņemt user approval**

Pēc T10 ziņas — gaidīt eksplicītu OK no lietotāja Telegrammā.

- [ ] **Step 2: Real deploy**

```
Set-Location "~\atmina\.worktrees\vad-phase-1.5"
& "C:\Program Files\Git\bin\bash.exe" scripts/deploy.sh
```

- [ ] **Step 3: Merge worktree → master**

Pēc deploy success:

```
Set-Location "~\atmina"
git checkout master
git merge --no-ff vad-phase-1.5
git push origin master
```

- [ ] **Step 4: Update wiki + CHANGELOG**

Append `wiki/CHANGELOG.md` jaunu entry zem 2026-05-02 ar Phase 1.5 fixes summary.
Atjaunot `wiki/index.md` VAD status uz "DEPLOYED 2026-05-02".

- [ ] **Step 5: Save memory**

`memory/project_vad_phase_0_1_done.md` → `project_vad_done.md` (rename, mark deployed).

- [ ] **Step 6: Worktree cleanup**

```
Set-Location "~\atmina"
git worktree remove .worktrees\vad-phase-1.5
git branch -d vad-phase-1.5
```

---

## Self-Review

**Spec coverage:**
- F13 (homonym contamination) → T2-T4 (disambig filter), T7 (curator), T9 (DELETE+reingest) ✓
- F14 (parse-fail retry) → T5 (reset_session + orchestrator retry) ✓
- F15 (logs/ mkdir) → T6 ✓
- Hosams Abu Meri (handoff §3) → T1 ✓
- § 9.5 ģimenes privacy — already implemented Phase 1, nav Phase 1.5 scope ✓
- § 15.2 audit trail jauns ieraksts — atstāts post-merge, ne plan fāzē ✓

**Placeholder scan:** Nav "TBD"/"TODO"/"fill in"/"Similar to Task N" — visi soļi ar pilnu kodu un komandām.

**Type consistency:**
- `_load_disambig_config` → `tuple[list[str], list[str]]` lietots T4 + T5 (caller code ne) ✓
- `_row_passes_disambig(row, hints, neg)` signatūra konsekventa ✓
- `client.reset_session()` definēts T5 step 3, izmantots T5 step 7 ✓
- `CONTAMINATED_PIDS` saraksts cleanup_contaminated_vad.py sakrīt ar HINTS pids seed_vad_disambig.py — verificēts manuāli ✓

**Risks:**
- Task 9 Step 3 re-ingest ~30-60 min — palaiž foreground vai background pēc operatora izvēles.
- Task 5 retry: ja BOTH first un retry fail, paliek esošā uzvedība (errors saraksta + skip). Acceptable degradation.
- Task 7 seed: ja `keywords` JSON jau ir cita formāta dict (nav `vad_disambig` lauks), seed pievieno lauku, ne pārraksta.
